# Scripts

Скрипты автоматизации: сборка, деплой, генерация файлов, проверки.

- `verify-site.sh` — быстрая проверка перед ответом, коммитом или пушем: ключевые файлы на месте, ассеты из HTML/CSS существуют, `styles.css` подключён, нет удалённых файлов в `git status`.
